@echo off
REM Copy this file to secrets.bat (which is gitignored) and fill in a real key.
REM Get a free API key (no credit card required) from https://aistudio.google.com/apikey
set GEMINI_API_KEY=your_key_here
